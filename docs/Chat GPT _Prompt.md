> _"Build POST <N> per the attached blueprint v2.0.0. Generation mode: single-image — you generate complete slides end-to-end. Therefore the deterministic_overlay_rule applies as an ACCURACY GATE, not a process rule: every text token in my content map must render exactly as written (spelling, casing, hex, punctuation); any deviation = regenerate that slide. Build slides sequentially 1→8, freeze each before the next. 1080×1620 portrait. Mascot per mascot_bible — reference image attached."_

**Two cosmetic notes (no action needed):** slide 5 step-4's recorded arrow direction is semantically odd (granted counts flow controller→host) — but the acceptance check already says arrows must follow data direction for future posts, so it self-corrects; and the `source_page_image` paths point to a Codex workspace — dead metadata, harmless.

**Your standing handoff per post (this is all you do now):** attach ① the blueprint JSON ② the mascot reference PNG ③ the preamble above ④ the content mapped to the 8 narrative roles (hook / context / analogy / mechanics / journey / star+rule / pitfalls / state+next) — **which is exactly what my per-post schematics produce.** So: I write the schematic, you paste, ChatGPT builds, I verify the rendered slides before you post.